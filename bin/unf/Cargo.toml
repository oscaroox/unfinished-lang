[package]
name = "unf"
version = "0.1.0"
authors = ["oscar <oscaroox@users.noreply.github.com>"]
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
ast = { path = "../../lib/ast" }
scanner = { path = "../../lib/scanner" }
parser = { path = "../../lib/parser" }
interpreter = { path = "../../lib/interpreter" }
typechecker = { path = "../../lib/typechecker" }

ariadne = "0.1.5"
rustyline = "9.1.2"
clap = { version = "3.1.6", features = ["derive"] }